#method 


Its a default method for the [[Shape]] Trait , which means any other Shape does not need to define it itself

Arguments :
It just takes a reference to a ray as an input and returns the [[Intersections]] object.